# Module 3: Object-Oriented Programming (OOP) & APIs

In Module 3, you'll learn how professional software is built using reusable blueprints (OOP) and how to make your code interact with live data on the web using HTTP APIs.

## 📦 Setup & Dependencies
Before running code in this module, install the required packages:
```bash
pip install -r requirements.txt
